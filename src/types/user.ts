export type UserType = {
  id: number;
  name: string;
  email: string;
  phone: string;
};

export type UserDetailType = {
  name: string;
  username: string;
  email: string;
  phone: string;
  website: string;
};

export type UserDetailProps = {
  id: number;
}